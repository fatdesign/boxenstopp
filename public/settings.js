// Settings for Boxenstopp Admin Dashboard
const SETTINGS = {
    // If you have a Cloudflare Worker Proxy to save to GitHub, put the URL here.
    // If empty, the admin dashboard operates in local mode and downloads the menu.json.
    proxyUrl: '',
    restaurantName: 'BOXENSTOPP'
};
